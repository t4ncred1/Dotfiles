# Global Gitignores
Gitignore common for many of the projects I'm working on, that should be set as the default global gitignore file after stowing it.
To set the file as the global gitignore, use `git config --global core.excludesfile <STOW_DIR>/.config/git/global-ignore`.
